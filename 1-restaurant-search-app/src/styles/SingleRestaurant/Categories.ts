import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    categoriesParent: {
        flexDirection: "row",
        alignItems: "center",
        marginVertical: 15,
    },
    categoriesText: {
        color: "#fff",
        fontSize: 17,
        marginLeft: 15,
    },
});
