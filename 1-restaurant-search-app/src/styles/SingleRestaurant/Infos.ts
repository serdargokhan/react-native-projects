import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    iconsWrapper: {
        flexDirection: "row",
        alignItems: "center",
        marginTop: 15,
    },
    textStyle: {
        color: "#fff",
        fontSize: 20,
        marginLeft: 15,
        maxWidth: "85%",
    },
});
