import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        justifyContent: "space-between",
    },
    title: {
        fontSize: 18,
        color: "gray",
    },
    sign: {
        fontSize: 18,
        color: "#f44d7f",
        borderBottomWidth: 2,
        borderBottomColor: "#f44d7f",
    },
});
