import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    headerText: {
        fontSize: 26,
        fontWeight: "700",
        letterSpacing: 1.15,
        color: "rgb(0, 0, 20)",
    },
    buttonContainer: {
        alignSelf: "flex-end",
        flex: 1,
    },
});
